<Window x:Class="GymManager.WPF.Views.Licensing.LicenseActivationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="GymManager - Activacion de Licencia"
        Height="500" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E293B">

    <Window.Resources>
        <!-- Estilos -->
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#64748B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#475569"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo y Titulo -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="GYM" FontSize="48" FontWeight="Bold" 
                       Foreground="#3B82F6" HorizontalAlignment="Center"/>
            <TextBlock Text="GymManager" Style="{StaticResource TitleStyle}"/>
            <TextBlock Text="Sistema de Gestion para Gimnasios" 
                       Foreground="#64748B" 
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Estado del Dongle -->
        <Border Grid.Row="1" 
                Background="#334155" 
                CornerRadius="8" 
                Padding="15"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Dispositivo de Seguridad" 
                               Foreground="#94A3B8" 
                               FontSize="12"/>
                    <TextBlock Text="{Binding DongleStatus}" 
                               Foreground="White" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        Content="Verificar"
                        Command="{Binding CheckDongleStatusCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="15,8"/>
            </Grid>
        </Border>

        <!-- Clave de Licencia -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Clave de Licencia" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource InputStyle}"
                     IsEnabled="{Binding IsDongleConnected}"/>
        </StackPanel>

        <!-- Mensaje de Estado -->
        <Border Grid.Row="3" 
                Background="#334155" 
                CornerRadius="8" 
                Padding="15"
                Margin="0,20,0,0">
            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="#F8FAFC" 
                       FontSize="13"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </Border>

        <!-- Informacion de Licencia -->
        <Border Grid.Row="4" 
                Background="#065F46" 
                CornerRadius="8" 
                Padding="15"
                Margin="0,15,0,0"
                Visibility="{Binding IsLicenseValid, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Licencia Activa" 
                           Foreground="#34D399" 
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,10"/>
                <TextBlock Text="{Binding LicenseInfo}" 
                           Foreground="#A7F3D0" 
                           FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- Botones -->
        <StackPanel Grid.Row="6" Orientation="Vertical" Margin="0,20,0,0">
            <Button Content="Activar Licencia"
                    Command="{Binding ActivateLicenseCommand}"
                    Style="{StaticResource ButtonStyle}"
                    IsEnabled="{Binding IsDongleConnected}"
                    Margin="0,0,0,10"/>

            <Button Content="Validar Licencia Existente"
                    Command="{Binding ValidateExistingLicenseCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    IsEnabled="{Binding IsDongleConnected}"/>
        </StackPanel>
    </Grid>
</Window>